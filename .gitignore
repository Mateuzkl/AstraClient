# --- Agent scratch documents (never commit) ----------------------------------
# Generated by Claude Code / workflow agents — local planning notes only.
AGENT_NOTES_*.md
ASTRA_UPGRADE_PLAN.md
NEXT_STEPS.md

# --- Project-specific (open source repo hygiene) ----------------------------
# Per-character persistence written by modules (loot prefs, hotkey state, etc.).
# Mirrors koliseu-otcv8 — added pre-emptively so future modules can write here
# without leaking player data into the public repo.
characterdata/

# Local per-user config — devs copy config.example.lua -> config.lua.
# Added pre-emptively to mirror koliseu-otcv8 conventions.
config.lua
# ...but the cavebot's config.lua is a shipped DEFAULT (the CaveBot.Config system),
# NOT per-user secrets. The bare rule above silently swallowed it, so it was never
# committed and fresh checkouts/worktrees/releases crashed on boot. Keep it tracked.
!mods/game_helper/cavebots/config.lua

# Worktrees / state created by the Claude Code harness — never commit.
.claude/

# --- Secrets: per-release asset key material ---------------------------------
# Generated by tools/gen_asset_key.py. Regenerate per release; NEVER commit.
# The C++ falls back to a committed dev default when this header is absent.
src/framework/core/keymaterial.gen.h

# Crash-dump analysis scratch (WinDbg symbol cache, minidumps).
crashdumps/

# Archived debug symbols ("symbol vault"): one subfolder per released build, each
# holding that build's KoliseuClient.pdb + exe so tools/symbolicate_crash.ps1 can
# turn a player's minidump into a source backtrace. Populated by tools/make_release.ps1.
# The PDBs are already caught by *.pdb, but the exe/build-info.txt here must not be
# committed either -> ignore the whole tree.
/symbols/

# --- Game assets -------------------------------------------------------------
# Legacy 8.60 assets: the .rar stays tracked as the canonical source, but the
# unpacked tree (Tibia.spr / Tibia.dat / pics) is regenerated locally and must
# not be committed.
data/things/860/

# Modern 15.24 assets: user-supplied (appearances.dat + sprite sheets).
# Never committed — devs drop their own copy in here.
data/things/1524/

# --- Build artefacts (Visual Studio vc17 / MSBuild) --------------------------
# OutDir writes the final binary + debug symbols straight into the repo root.
AstraClient_*.exe
KoliseuClient*.exe
*.pdb
*.ilk
*.exp
*.lib

# Intermediate + output dirs produced by the vc17 solution.
vc17/x64/
vc17/Win32/
vc17/.vs/
vc17/Debug/
vc17/Release/
vc17/Debug_lib/
vc17/Release_lib/
vc17/DirectX/
vc17/OpenGL/

# vcpkg manifest-mode install tree.
vcpkg_installed/

# --- Build artefacts (CMake, in case someone wires it up) --------------------
build/
out/
CMakeFiles/
CMakeCache.txt
CMakeUserPresets.json
cmake_install.cmake
Makefile

# --- Logs --------------------------------------------------------------------
*.log
console-trace.log
packet*.log
otclient*.log

# --- Visual Studio user/temp files -------------------------------------------
*.suo
*.user
*.sln.docstates
*.vcxproj.user

# MSBuild intermediate output
*_i.c
*_p.c
*.meta
*.obj
*.pch
*.pgc
*.pgd
*.rsp
*.sbr
*.tlb
*.tli
*.tlh
*.tmp
*.tmp_proj
*.vspscc
*.vssscc
.builds
*.pidb
*.scc

# Visual C++ cache files
ipch/
*.aps
*.ncb
*.opensdf
*.sdf
*.cachefile

# Visual Studio profiler
*.psess
*.vsp
*.vspx

# Generic build leftovers
[Dd]ebug/
[Rr]elease/
x64/
[Bb]in/
[Oo]bj/
[Tt]est[Rr]esult*/
[Bb]uild[Ll]og.*

# --- Editor / tooling --------------------------------------------------------
.vs/
.vscode/
.idea/
.lvimrc
.clang_complete
*.swp
*.bak
*~
*.kate-swap
*.autosave
CMakeLists.txt.user*
tags
Thumbs.db
.directory

# --- Vendored Google ANGLE (third_party/angle) -------------------------------
# Keep the prebuilt ANGLE import libs and DLLs tracked despite the generic
# *.lib / x64/ / bin/ rules above. Parent dirs must be re-included before files.
!third_party/angle/lib/
!third_party/angle/lib/x64/
!third_party/angle/lib/x64/*.lib
!third_party/angle/lib/x64/*.def
!third_party/angle/bin/
!third_party/angle/bin/x64/
!third_party/angle/bin/x64/*.dll

# Deployed ANGLE/Vulkan runtime DLLs copied next to the (ignored) exe by the
# build's post-build step. Reproducible from third_party/angle, so don't track.
/libEGL.dll
/libGLESv2.dll
/d3dcompiler_47.dll
/vulkan-1.dll

# --- Archives that shouldn't be committed (allowlist 860.rar) ----------------
*.zip
*.7z
*.gz
*.rar
!data/things/860.rar

# Keep this file tracked regardless of patterns above.
!.gitignore
